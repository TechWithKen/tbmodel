[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "tbmodel"
version = "1.0"

dependencies = [
    "numpy",
    "pandas",
    "scikit-learn",
    "xgboost",
    "joblib"
]

[project.scripts]
tbmodel = "TUBERCULOSIS_EXPERIMENT.cli:main"